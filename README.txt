install script assumes working directory is /home/<user>/shellsettings

install script overwrites existing .bashrc and .vimrc files.


Git command to store credentials:   git config --global credential.helper store

